import 'package:shadcn_flutter/shadcn_flutter.dart';

class DashboardLayout extends StatelessWidget {
  const DashboardLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(child: Placeholder());
  }
}
